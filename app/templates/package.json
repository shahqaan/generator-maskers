{
  "name": "<%= name %>",
  "version": "0.1.0",
  "description": "<%= description %>",
  "main": "index.js",
  "license": "ISC",
  "scripts": {
    "start": "node server.js | bunyan",
    "postinstall": "gulp",
    "test": "grunt test",
    "build": "grunt build",
    "all": "npm run build && npm run test",
    "migrate": "sequelize db:migrate | bunyan",
    "dev-start": "nodemon server.js | bunyan --output short --color"
  },
  "dependencies": {},
  "devDependencies": {}
}
